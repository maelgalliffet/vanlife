{
  "name": "vanlife-weekend-booking",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "bash ./scripts/dev.sh",
    "build": "npm run build -w apps/api && npm run build -w apps/web",
    "start": "npm run start -w apps/api",
    "lint": "npm run lint -w apps/api && npm run lint -w apps/web"
  }
}
